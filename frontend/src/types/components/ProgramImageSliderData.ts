import { BaseStrapiComponent, StrapiImage } from './ProgramSliderData';

export interface ProgramImageSliderData extends BaseStrapiComponent {
	__component: 'program-components.dialogue-slider';
	images?: StrapiImage[];
}
